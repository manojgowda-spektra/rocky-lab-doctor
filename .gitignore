# secrets — never commit (live Azure OpenAI key lives here locally)
.env.local
.env
*.env.local
# runtime artifacts
source/rocky-prototype/logs/
node_modules/
*.zip
# except the Copilot extension bundle — the submission tells judges to download it
!Rocky-Extension.zip
# local editor/agent settings
.claude/
TeamAlpha-Submission/
