# secrets — never commit (live Azure OpenAI key lives here locally)
.env.local
.env
*.env.local
# runtime artifacts
source/rocky-prototype/logs/
node_modules/
*.zip
# except the Copilot extension bundle — the submission tells judges to download it
!Rocky-Extension.zip
# local editor/agent settings
.claude/
TeamAlpha-Submission/
vmpackage/dist/
vmpackage/test/rocky-proof.png
vmpackage/test/rocky-wrongclick.png
vmpackage/test/rocky-error.png
# compiled by tools/build-knowledge.js - not source
vmpackage/webext/knowledge/cloudlabs-kb*.json
vmpackage/webext/knowledge/cloudlabs-vec.json

# The AI key. Endpoint and deployment are committed (they are not secret); the key is not.
# This repo is PUBLIC - a live Azure key pushed here is scraped within minutes and revoked.
vmpackage/webext/ai.local.json
